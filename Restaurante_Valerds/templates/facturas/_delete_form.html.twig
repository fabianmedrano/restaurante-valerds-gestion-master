<form method="post" action="{{ path('facturas_delete', {'idFactura': factura.idFactura}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factura.idFactura) }}">
    <button class="btn">Delete</button>
</form>
