<form method="post" action="{{ path('mesa_delete', {'idMesa': mesa.idMesa}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mesa.idMesa) }}">
    <button class="btn">Delete</button>
</form>
